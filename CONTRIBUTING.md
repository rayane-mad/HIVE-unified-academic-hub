## Contributing

This repository was built as a collaborative academic project.

### Local setup
- Follow the README to run the 3 services (backend, frontend, logic-engine).
- Copy `backend/.env.example` to `backend/.env` and fill values (do not commit secrets).

### Notes
- Keep commits small and descriptive.
- Document any API or schema changes.
